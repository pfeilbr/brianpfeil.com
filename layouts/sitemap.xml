{{- /* Hugo's built-in sitemap, except hreflang uses the bare language code
   ("en", "zh") like the <link rel=alternate> tags in baseof.html. The
   built-in one uses the locale ("en-us", "pt-br"), which both disagrees with
   the page heads and tells search engines each translation is for one
   country only. x-default points at the English page, as in the heads. */ -}}
{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- range where .Pages "Sitemap.Disable" "ne" true }}
    {{- if .Permalink }}
  <url>
    <loc>{{ .Permalink }}</loc>{{ if not .Lastmod.IsZero }}
    <lastmod>{{ safeHTML (.Lastmod.Format "2006-01-02T15:04:05-07:00") }}</lastmod>{{ end }}{{ with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>{{ end }}{{ if ge .Sitemap.Priority 0.0 }}
    <priority>{{ .Sitemap.Priority }}</priority>{{ end }}
    {{- if .IsTranslated }}
      {{- range .AllTranslations }}
    <xhtml:link rel="alternate" hreflang="{{ .Language.Lang }}" href="{{ .Permalink }}"/>
      {{- end }}
      {{- range .AllTranslations }}{{ if eq .Language.Lang hugo.Sites.Default.Language.Lang }}
    <xhtml:link rel="alternate" hreflang="x-default" href="{{ .Permalink }}"/>
      {{- end }}{{ end }}
    {{- end }}
  </url>
    {{- end }}
  {{- end }}
</urlset>
